# Install devDependencies even when NODE_ENV=production (Render sets it), so the
# `postinstall` build has `tsc` available and `dist/` is produced on deploy.
production=false
include=dev
engine-strict=false
